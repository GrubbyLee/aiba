# Workflow Approval Capability

`workflow-approval` defines an explicit state machine rather than a UI or table
template. Trusted definitions control resource type, approver eligibility,
required count, and self-approval policy. Decisions are attributable, immutable,
revision checked, idempotent, and terminal states cannot be reopened.
