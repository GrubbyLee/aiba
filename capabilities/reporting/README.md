# Reporting Capability

`reporting` selects server-owned report definitions; it does not accept SQL,
templates, fields, or output paths. Definitions control parameters, formats,
projection, row limits, timeout, and storage. Outputs become private file assets
and portable records expose only lifecycle, counts, and opaque asset identity.
