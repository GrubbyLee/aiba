# Feature Flags Capability

`feature-flags` defines deterministic evaluation, not a flag dashboard or SDK.
Tenant, stable subject, targeting attributes, rollout salt, and policy come from
trusted server state. Results expose only the selected variant, reason, policy
revision, and evaluation time.
