# Secure Workspace Solution

This domain-neutral Solution binds an exact, dependency-ordered security and
application foundation. It does not define business entities, storage, or UI.

Check a project without changing it:

```bash
aiba compose secure-workspace --root .
```

Every constituent remains independently verified. The Solution cannot ignore
or override capability invariants.
