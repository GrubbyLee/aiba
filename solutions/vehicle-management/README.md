# Vehicle Management Solution

This solution binds an exact, dependency-ordered set of AIBA capabilities for a
vehicle administration product. It includes the `vehicle-records` domain
contract rather than presenting a generic administration bundle as a complete
industry solution.

Check a project without changing it:

```bash
aiba compose vehicle-management --root .
```

Every constituent remains independently verified. The solution cannot ignore
or override capability invariants.
