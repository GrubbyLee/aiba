{
  "name": "@aiba/fixture-identity-reference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "aiba-spec": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
