{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aiba.dev/schema/interfaces/v0alpha1/data-export-command.schema.json",
  "title": "AIBA Data Export Command",
  "type": "object",
  "additionalProperties": false,
  "required": ["profileId", "idempotencyKey"],
  "properties": {
    "profileId": { "type": "string", "pattern": "^[a-z][a-z0-9-]{1,95}$" },
    "idempotencyKey": { "type": "string", "minLength": 16, "maxLength": 255 }
  }
}
