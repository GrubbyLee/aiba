{
  "name": "@aiba/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "bin": {
    "aiba": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aiba/core": "workspace:*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
